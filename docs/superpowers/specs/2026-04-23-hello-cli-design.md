# Hello CLI Command

Add a `whisper hello` subcommand in `packages/cli/src/commands/` that prints `hello` to stdout and exits 0.
No args, no flags. Wire into the command registry. Add one vitest test asserting stdout.
